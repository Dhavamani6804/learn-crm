spring:
  application:
    name: crmdemo
  datasource:
    url: jdbc:mysql://localhost:3306/crm
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  mongodb:
    uri: mongodb://localhost:27017/crm